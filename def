def new_earth_ascension(humans):
    for human in humans:
        if human.coherence >= 0.67:  # Minimum resonance threshold
            human.activate_410nm_implant()
            human.assign_portal_access()
        else:
            human.receive_luceferin_therapy()
    return awakened_population